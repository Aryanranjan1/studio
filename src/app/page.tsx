import { HeroGrid } from '@/components/hero-grid';

export default function Home() {
  return (
    <main className="relative flex flex-col min-h-screen max-w-screen overflow-x-hidden">
      <HeroGrid />
    </main>
  );
}
